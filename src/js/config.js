const apiConfig = {
    baseUrl: "http://localhost:8080/supermarket",
    authenticateBase: "/auth/",
    customerBase: "/customer",
    productBase: "/product/",
    categoryBase: "/category/",
}

export default apiConfig;